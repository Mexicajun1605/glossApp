<p style="color: green"><%= notice %></p>

<h1>Glosses</h1>

<%= link_to "New gloss", new_gloss_path, class: "btn btn-secondary"%>

<br>
<br>

<%= search_form_for @q do |f| %>

  <%= f.label :concepto_cont %>
  <%= f.search_field :concepto_cont %>

  <%= f.submit %>
<% end %>

  <table class="table table-striped table-bordered table-hover">
  <thead class="thead-dark">
        <tr>
          <th>Concepto</th>
          <th>Traducción</th>
          <th>Definición</th>
          <th>Enlace</th>
          <th>Borrar</th>
        </tr>
      </thead>
  <tbody>
  <div id="glosses">
  
  <% @glosses.each do |gloss| %>
  <tr>
            <td><%= link_to gloss.concepto, gloss %></td>
            <td><%= gloss.traducción %></td>
            <td><%= gloss.definición %></td>
            <td><%= gloss.enlaces_de_referencia %>
            <td><%= button_to 'Borrar', gloss, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-danger btn-sm" %></td>
  </tr>

    <% end %>
</div>

  </tbody>